apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
type: Opaque
data:
  username: ${MONGODB_ROOT_USERNAME}
  password: ${MONGODB_ROOT_PASSWORD}
  photomapUser: ${MONGODB_APP_USERNAME}
  photomapPassword: ${MONGODB_APP_PASSWORD}