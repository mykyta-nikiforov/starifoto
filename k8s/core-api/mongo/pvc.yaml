apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-data-pvc
spec:
  storageClassName: standard
  volumeName: pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi